<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= id %> Profile</title>
    <link rel="stylesheet" href="/profile.css" />
  </head>
  <body>
    <h1 class="centered-text"><%= id %> Profile</h1>
    <img style="width: 100%" src="../<%= id %>/banner.png" />

    <img
      class="profile-img"
      src="../<%= id %>/profile.png"
      alt="Profile Image"
    />

    <div class="container">
      <section>
        <% // Assuming you have the profile information as one string const
        profileString = "Real Name:GOSIG GOLDEN by Ikea:Birthdate/'Gotcha
        Day':Born © 2001 in China, but found at a rummage sale in Seattle, WA in
        March of 2012.:Powers:As Douglas Adams once said 'One is never alone
        with a rubber duck.' Mickey, the Debug Dog is a patient listener, able
        to help you solve your debugging woes. Unlike a rubber duck, Mickey is
        also soft. Mickeyly, doubling as a pillow for those occasional times you
        need to just close your eyes for a 5 minute power nap.:Stats:Real name
        GOSIG GOLDEN Prefered pronouns:They/Them Size:12' x 16' X 12' Weight:2
        lbs Siblings: Francesca (Dark Purple), Bartholomew (Light Green),
        Marcello (Blue), Cordelia (Dark Green), Guinevier (Gold)"; // Split the
        string into an array of key-value pairs const profileArray =
        profileString.split(':'); // Remove any leading/trailing whitespaces
        const trimmedProfileArray = profileArray.map(item => item.trim()); //
        Group the array into pairs const groupedProfileArray = []; for (let i =
        0; i < trimmedProfileArray.length; i += 2) {
        groupedProfileArray.push([trimmedProfileArray[i], trimmedProfileArray[i
        + 1]]); %>

        <div class="grid-container">
          <% groupedProfileArray.forEach(pair => { %>
          <p class="grid-item"><%= pair[0] %></p>
          <p class="grid-item"><%= pair[1] %></p>
          <% }); %>
        </div>

        <% // Close the EJS block %>
      </section>
    </div>
    <div class="center-content">
      <div><%- bioText %></div>
      <img src="../<%= id %>/image1.png" />
    </div>

    <img class="profile-img" src="your-profile-image.jpg" alt="Profile Image" />
    <p>Profile ID: <%= id %></p>
    <p>This is your profile page. Customize it as you like!</p>
  </body>
</html>
